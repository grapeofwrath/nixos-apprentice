# Copy the generated hardware-configuration.nix file from your initial install, replacing your hostName and config location if different:

# cp /etc/nixos/hardware-configuration.nix ~/nixos-apprentice/nixos/your-hostName/
